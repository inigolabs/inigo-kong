_format_version: "3.0"
_transform: true

services:
  - host: host.docker.internal
    name: starwars
    port: 30013
    protocol: http
    routes:
      - name: query
        paths:
          - /query
        strip_path: false
